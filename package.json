{
  "name": "anti-gravity-app",
  "version": "1.0.0",
  "dependencies": {
    "firebase": "^12.12.1"
  },
  "scripts": {
    "build": "cd backend && npm install",
    "start": "cd backend && npm start"
  }
}
